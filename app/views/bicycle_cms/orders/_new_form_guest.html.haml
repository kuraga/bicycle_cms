= semantic_nested_form_for order, url: order_path do |form|
  = form.inputs do
    = form.input :name
    = form.input :email
    = form.input :comment
  = form.actions do
    = form.action :submit
    = form.action :cancel
