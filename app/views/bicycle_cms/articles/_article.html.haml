= div_for article do
  - unless article.is_page?
    .properties
      = render_properties_for article
  - if article.thumbnail.present?
    .thumbnail
      = link_to image_tag(article.thumbnail.medium.url), article.thumbnail.url
  .body
    = eval_in_erb_sandbox article.body.presense(article.description).html_safe, binding
