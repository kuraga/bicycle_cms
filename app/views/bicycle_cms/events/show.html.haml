= render_panel_for @event

= render @event

- if @event.is_commentable or signed_in_as_admin?
  .comments
    %h3= t('bicycle_cms/comments.main.bicycle_cms/comments')
    = render(@event.comments) || t('general.messages.empty')

  = render 'bicycle_cms/comments/new'
